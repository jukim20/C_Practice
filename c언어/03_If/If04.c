#include <stdio.h>
void main()
{
	int num1 , num2; int my_num , com_num;
	num1 = 20;
	num2 = 10;
	if(num1 > num2){
		printf("num1 이 num2 보다 크다\n");
		printf("num2 는 num1 보다 작다\n");
	}
	if(num1 < num2){
		printf("num1 이 num2 보다 작다\n");
		printf("num2 는 num1 보다 크다\n");
	}
	//문제 1) ?? 가 ?? 보다 크다 출력 
	//가위바위보게임) //com_num은 항상 바위(2) 를 낸다. 
	//				   my_num 과 비교해서 이겻다 젓다 비겻다 출력
	printf("가위(1) 바위(2) 보(3) 를 입력하세요 >>>");
	if(10==10){
		if(20==20){
		}
	}
}